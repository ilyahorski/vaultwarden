/**
 * Централизованная конфигурация тайлов для Excalibur.js
 *
 * Этот файл является единственным источником истины для размеров тайлов
 * во всей системе Excalibur. Все другие файлы должны импортировать
 * константы отсюда вместо использования hardcoded значений.
 */

export const TILE_CONFIG = {
  /**
   * Размер тайла в пикселях
   * Изменено с 32x32 на 16x16 для поддержки профессиональных тайлсетов
   */
  TILE_SIZE: 16,

  /**
   * Половина размера тайла (для центрирования объектов)
   */
  TILE_SIZE_HALF: 8,

  /**
   * Масштаб для ретро-графики (1.0 = без масштабирования)
   */
  TILE_SCALE: 1.0,
} as const;
